<!DOCTYPE html>
<html>
  <head>
    <title>List <%=shopName%></title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>About <%=shopName%></h1>
    <p>Here are the books we sell:</p>

    <ul>
      <% for (let book of availableBooks) { %>
      <li><%= book.name%> - <%= book.price %></li>
      <% } %>
    </ul>
  </body>
</html>
